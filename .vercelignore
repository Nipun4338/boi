# Exclude only the root vendor folder (managed by composer)
/vendor/
sql/
.git/
node_modules/
.phpintel/

# We DO NOT ignore 'images/' and 'admin/vendor/' 
# because they contain static assets that must be uploaded.
# specifically, admin/vendor/ has the CSS/JS for the theme.
